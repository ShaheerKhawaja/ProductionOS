#!/usr/bin/env bash
# ProductionOS — review logger
set -euo pipefail
STATE_DIR="${PRODUCTIONOS_HOME:-$HOME/.productionos}"
mkdir -p "$STATE_DIR/analytics"
LOG="$STATE_DIR/analytics/review-log.jsonl"
echo "$1" >> "$LOG"
echo "Review logged to $LOG"
